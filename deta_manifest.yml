v: 1
micros:
    - name: pampu-backend
      directory: pampu-backend
      engine: python3.9
    - name: pampu-frontend
      directory: pampu-frontend
      engine: create-react-app
      command: npm run build
      publish: build
      primary: true