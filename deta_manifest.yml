v: 1
micros:
    - name: pampu-frontend
      directory: pampu-frontend
      engine: create-react-app
      command: npm run build
      publish: build
      primary: true
    - name: pampu-backend
      directory: pampu-backend
      engine: python3.9
